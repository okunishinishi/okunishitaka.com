#!/usr/bin/env node

/**
 * @file Run scenario tests
 */

"use strict";

var h = require('../_helper'),
    configs = require('../configs'),
    async = h.async,
    tasks = h.tasks;

process.chdir(h.basedir);

async.eachSeries([
    {
        config: configs.forkConfig.testScenarioTests,
        task: tasks.ForkTask
    }
], function (data, callback) {
    new (data.task)(data.config).run(callback);
}, h.done('All test done!', true));