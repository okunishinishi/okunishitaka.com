/**
 * @file App schemas.
 * @module okunishitaka.com/app/schemas
 */

"use strict";

var u = require('apeman-util'),
    JsonSchema = u.schemas.JsonSchema;

module.exports = {
    {{#each schemas}}get {{exportsName}}() {
        return new JsonSchema(require('./{{requireName}}'));
    }{{#if @last}}{{else}},
    {{/if}}{{/each}}
}
