/**
 * @ngdoc object
 * @name {{Name}}ViewingDatasource
 * @description View data source for {{name}}.
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{Name}}ViewingDatasource', function (ViewingDatasource, {{Name}}Entity, {{name}}ApiService) {
            return ViewingDatasource.define(
                /** @lends {{Name}}ViewingDatasource.prototype */
                {
                    _oneRequest: function (id, callback) {
{{#if singleton}}                        {{name}}ApiService.singleton(callback);
{{else}}                        {{name}}ApiService.one(id, callback);
{{/if}}                    },
                    _parseData: function (data) {
                        return {{Name}}Entity.new(data);
                    }
                }
            );
        });

})(angular, apeman);