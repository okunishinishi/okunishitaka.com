/**
 * @ngdoc object
 * @name {{Name}}DestroyingDatasource
 * @description Destoying data source for {{name}}.
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{Name}}DestroyingDatasource', function (DestroyingDatasource, {{Name}}Entity, {{name}}ApiService) {
            return DestroyingDatasource.define(
                /** @lends {{Name}}DestroyingDatasource.prototype */
                {
{{#if destroy}}                    _oneRequest: function (id, callback) {
                        {{name}}ApiService.one(id, callback);
                    },
                    _destroyRequest: function (id, callback) {
                        {{name}}ApiService.destroy(id, callback);
                    },
{{/if}}
                }
            );
        });
})(angular, apeman);