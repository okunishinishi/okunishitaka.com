/**
 * One data source for {{name}}.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('ok.datasources')
        .factory('{{Name}}OneDatasource', function (OneDatasource, {{Name}}Entity, {{name}}ApiService) {
            return OneDatasource.define(
                /** @lends {{Name}}OneDatasource.prototype */
                {
                    _oneRequest: function (id, callback) {
                        {{name}}ApiService.one(id, callback);
                    },
                    _createRequest: function (data, callback) {
                        {{name}}ApiService.create(data, callback);
                    },
                    _updateRequest: function (id, data, callback) {
                        {{name}}ApiService.update(id, data, callback);
                    },
                    _destroyRequest: function (id, callback) {
                        {{name}}ApiService.destroy(id, callback);
                    },
                    _parseData: function (data) {
                        return data.map({{Name}}Entity.new);
                    }
                }
            );
        });
});