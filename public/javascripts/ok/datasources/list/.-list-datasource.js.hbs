/**
 * List data source for {{name}}.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('ok.datasources')
        .factory('{{Name}}ListDatasource', function (ListDatasource, {{Name}}Entity, {{name}}ApiService) {
            return ListDatasource.define({
                _getRequest: function (query, callback) {
                    query._sort = '_at';
                    query._reverse = 'true';
                    {{name}}ApiService.list(query, callback);
                },
                _parse: function (data) {
                    return data.map({{Name}}Entity.new);
                }
            });
        });

})(angular, apeman);