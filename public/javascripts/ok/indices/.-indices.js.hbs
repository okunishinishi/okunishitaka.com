/**
 * {{description}}.
 * @requires angular
 * @requires apeman
 */
(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{name}}', function define{{Name}}($injector) {
            return {
                {{#each subModules}}get {{this}}() { return $injector.get('{{this}}'); }{{#if @last}}{{else}},
                {{/if}}{{/each}}
            };
        });
})(angular);