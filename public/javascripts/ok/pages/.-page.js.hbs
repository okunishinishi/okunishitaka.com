/**
* Page script for {{name}}.
* @requires angular
*/

(function (ng) {
    "use strict";

    ng
        .module('{{prefix}}.{{name}}Page', [
            '{{prefix}}.page',
        ])
        .run(function ($rootScope) {
            $rootScope.page = '{{name}}';
        })
        .controller('{{Name}}Ctrl', function ($scope) {

        });

})(angular);