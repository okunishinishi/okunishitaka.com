/**
 * @ngdoc module
 * @module '{{prefix}}.{{name}}Page'
 * @description Page script for {{name}}.
 */
(function (ng) {
    "use strict";

    ng
        .module('{{prefix}}.{{name}}Page', [
            '{{prefix}}.page',
        ])
        .run(function ($rootScope) {
            $rootScope.page = '{{name}}';
        })
        .controller('{{Name}}Ctrl', function ($scope) {

        });

})(angular);