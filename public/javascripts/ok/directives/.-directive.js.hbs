/**
 * {{description}}.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .directive('{{name}}', function define{{Name}}() {
            return {

            }
        });

})(angular, apeman);