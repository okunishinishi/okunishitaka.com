/**
 * {{description}}.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{name}}', function {{Name}}() {
            var s = this;

        });

})(angular, apeman);