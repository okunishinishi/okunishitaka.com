/**
 * {{Name}} api service.
 * @requires angular
 */
(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .service('{{name}}ApiService', function {{Name}}ApiService ($http) {
            var s = this;
        });

})(angular);