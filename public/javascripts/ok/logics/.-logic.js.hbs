/**
 * {{Name}} Logic.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('ok.logics')
        .factory('{{name}}Logic', function {{name}}Logic() {
            return {

            }
        });
})(angular, apeman);