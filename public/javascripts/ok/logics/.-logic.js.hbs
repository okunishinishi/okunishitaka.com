/**
 * {{Name}} Logic.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('ok.logics')
        .factory('{{name}}Logic', function define{{Name}}Logic() {
            return {

            }
        });
})(angular, apeman);