/**
 * @ngdoc filter
 * @filter {{name}}
 * @description {{description}}
 */

(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .filter('{{name}}', function define{{Name}}() {
            return function {{name}}(input) {

            };
        });
})(angular, apeman);