/**
 * @ngdoc filter
 * @filter {{name}}
 * @description {{description}}
 */

(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .filter('{{name}}', function define{{Name}}(input) {
            return function {{name}} (input) {

            };
        });
}, apeman, apeman);