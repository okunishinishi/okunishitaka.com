/**
 * {{Name}} entity.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{Name}}Entity', function (Entity) {
            function {{Name}}Entity() {
                var s = this;
                s.init.apply(s, arguments);
            }

            {{Name}}Entity.prototype = new Entity({});

            return {{Name}}Entity;
        })
})(angular, apeman);