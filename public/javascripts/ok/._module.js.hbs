/**
 * {{prefix}} {{namespace}} module.
 * @requires angular
 */

(function (ng) {
    "use strict";
    ng
        .module('{{prefix}}.{{namespace}}', [
            {{#each dependencies}}{{this}}{{#if @last}}{{else}},
            {{/if}}{{/each}}
        ]);
})(angular);
