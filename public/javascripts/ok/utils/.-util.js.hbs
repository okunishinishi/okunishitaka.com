/**
 * {{Name}} utility.
 * @requires angular
 * @requires apeman
 */
(function (ng, ap) {
    "use strict";

    ng
        .module('ok.utils')
        .factory('{{name}}Util', function () {
            return {

            }
        })
})(angular, apeman);