/**
 * Bud file for models.
 */

"use strict";

var u = require('apeman-util'),
    format = u.core.util.format,
    string = u.string;

var meta = require('../.meta');

module.exports = meta.models.map(function (name) {
    return {
        mode: '644',
        force: false,
        path: [
            __dirname,
            (string.paramString(name) + '.js')
        ].join('/'),
        data: {
            module: 'ok.models',
            name: string.camelString(name),
            Name: string.pascalString(name)
        }
    }
});
