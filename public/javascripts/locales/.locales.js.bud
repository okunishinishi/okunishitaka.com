/**
 * Bud file for l.js
 */

"use strict";

var u = require('apeman-util'),
    path = u.core.path;

module.exports = {
    mode: '444',
    force: true,
    path: path.resolve(__dirname, '..', 'locales.js'),
    data: {
        namespace: 'locales',
        get locales() {
            var locales = require('../../../app/locales/index');
            return JSON.stringify(locales, null, 4);
        }
    }
}

