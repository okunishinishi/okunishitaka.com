/**
 * Test for {{name}}
 * Runs with karma.
 */

"use strict";


var ng = angular,
    mock = ng.mock;

describe('{{name}}Index', function () {

    beforeEach(mock.module('ok.indices'));
    it('Inject the index.', inject(function ({{name}}Index) {
        expect({{name}}Index).toBeDefined();
        Object.keys({{name}}Index).forEach(function(key){
            expect({{name}}Index[key]).toBeDefined();
        });
    }));
});