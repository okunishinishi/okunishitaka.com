/**
* Test for {{name}} utility.
* Runs with karma.
*/

"use strict";


var ng = angular,
    mock = ng.mock;

describe('{{name}}util', function () {

    beforeEach(mock.module('ok.utils'));

    it('Inject a util', inject(function ({{name}}Util) {
        expect({{name}}Util).toBeDefined();
    }));

});