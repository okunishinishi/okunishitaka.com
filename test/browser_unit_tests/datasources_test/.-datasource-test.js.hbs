/**
 * Test for {{name}} data source.
 * Runs with karma.
 */

"use strict";


var ng = angular,
    mock = ng.mock;

describe('{{Name}}Datasource', function () {

    beforeEach(mock.module('ok.datasources'));

    it('Initalize a data source', inject(function ({{Name}}Datasource) {
        var datasource = new {{Name}}Datasource({});
        expect(datasource).toBeDefined();
    }));

});