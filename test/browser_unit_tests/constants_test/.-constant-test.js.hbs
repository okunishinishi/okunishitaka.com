/**
 * Test for {{name}}
 * Runs with karma.
 */

"use strict";


var ng = angular,
    mock = ng.mock;

describe('{{name}}Constant', function () {
    beforeEach(mock.module('ok.constants'));
    it('Inject a service.', inject(function ({{name}}Constant) {
        expect({{name}}Constant).toBeDefined();
    }));
});