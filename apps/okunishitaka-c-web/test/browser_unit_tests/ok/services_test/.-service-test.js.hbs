/**
 * Test for {{name}}
 * Runs with karma.
 */

"use strict";


var ng = angular,
    mock = ng.mock;

describe('{{name}}Service', function () {
    var {{name}}Service;
    beforeEach(function () {
        mock.module('{{prefix}}.services');
        inject(['{{name}}Service', function (injected) {
            {{name}}Service= injected;
        }]);
    });

    it('Inject the service.', function () {
        expect({{name}}Service).toBeDefined();
    });
});