/**
 * Schema for create {{model}}.
 * @module {{module}}/app/schemas/{{model}}CreateSchema
 */
"use strict";

var _defs = require('./_def');

module.exports = Object.seal(
    {
        title: '{{title}}',
        type: "object",
        properties: {},
        required: []
    }
);