/**
 * @ngdoc object
 * @name {{Name}}
 * @description {{description}}
 */

(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{Name}}', function define{{Name}}() {
            function {{Name}}() {

            }

            {{Name}}.prototype = {
            };

            return {{Name}};
        });
})(angular);