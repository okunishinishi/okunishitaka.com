/**
 * @ngdoc object
 * @name {{name}}
 * @description {{description}}.
 */
(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .factory('{{name}}', function define{{Name}}() {
            return {

            }
        });
})(angular);