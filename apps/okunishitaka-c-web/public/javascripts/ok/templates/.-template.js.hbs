/**
 * @ngdoc object
 * @name {{name}}Template
 * @description Template for {{name}}
 */
(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .value('{{name}}Template', {{{data}}});

})(angular);