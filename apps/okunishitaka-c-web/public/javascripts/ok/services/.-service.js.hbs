/**
 * @ngdoc object
 * @name {{name}}
 * @description {{description}}.
 */
(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .service('{{name}}', function {{Name}}() {
            var s = this;

        });

})(angular);