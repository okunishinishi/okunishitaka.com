/**
 * @ngdoc filter
 * @filter {{name}}
 * @description {{description}}
 */

(function (ng) {
    "use strict";

    ng
        .module('{{module}}')
        .filter('{{name}}', function define{{Name}}() {
            return function {{name}}(input) {

            };
        });
})(angular);